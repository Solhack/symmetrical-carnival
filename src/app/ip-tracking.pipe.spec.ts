import { IpTrackingPipe } from './ip-tracking.pipe';

describe('IpTrackingPipe', () => {
  it('create an instance', () => {
    const pipe = new IpTrackingPipe();
    expect(pipe).toBeTruthy();
  });
});
