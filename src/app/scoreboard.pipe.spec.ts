import { ScoreboardPipe } from './scoreboard.pipe';

describe('ScoreboardPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreboardPipe();
    expect(pipe).toBeTruthy();
  });
});
